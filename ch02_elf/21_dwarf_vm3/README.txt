dwarf2と同様に、`buf`のアドレスなどがバイナリに依存するため、このディレクトリのコードがそのままどの環境でも動くと限らないことに注意。
また、脚注にも記載がある通り、dwarf3ではROPを利用するが、libgccが後続の関数呼び出しでスタックのどのオフセットを上書きするかなどによって、うまく動作するかどうかが変わってくる。したがって、添付したDocker環境上での検証が推奨される。
添付した環境を用いてもなお、aptのパッケージの更新などにより、libgccに変更が入った場合、動作しなくなる可能性がある。
